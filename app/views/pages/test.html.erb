<script>
    function get_short_url(long_url) {
        var login = "flashingdeals";
        var api_key = "R_8b0537e5029cb7e4f83c48bab1a66eba";
        $.getJSON("http://api.bitly.com/v3/shorten?callback=?", {
            "format": "json",
            "apikey": api_key,
            "login": login,
            "longUrl": long_url
        },
        function(response) {
            $('#content').append(response.data.url);
        });
    };

    get_short_url("http://www.flashingdeals.com/deals/einstein-bros-bagels-4-off-bagel-bucket")
</script>