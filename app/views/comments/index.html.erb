<div id="side_panel">
	<%= render "shared/control_panel_s" %>
</div>
<div id="comments_index">
	<div class="clear"></div>
	<%= render :partial => "comment_index", :collection => @comments, :as => :comment %>
	<%= will_paginate @comments %>
</div>