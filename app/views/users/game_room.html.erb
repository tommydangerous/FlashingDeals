<div id="game_room_left">
	<div id="game_room_user">
		<div id="game_room_user_image">
			<%= link_to @user do %>
				<%= image_tag @user.photo.url %>
			<% end %>
		</div>
		<div id="game_room_user_points">
			<div id="my_points">
				<div id="my_points_next_level">
					<%= @user.points %><span class="grey_200">/</span><%= @user.next_level %>
				</div>
				<div id="my_points_title">
					<p>Level <%= @user.level %> <%= @user.title %></p>
				</div>
				<div id="my_points_progress" title="<%= (@user.progress.round(2) * 100).round %>%">
					<%= image_tag "/assets/my_points_progress.jpg" %>
				</div>
			</div>
		</div>
	</div>
	<div id="how_to_level">
		<p id="how_to_level_header">How To Level</p>
		<div id="how_to_level_body">
			<p>You can score points by doing many different things.</p>
		</div>
	</div>
</div>
<div id="leaderboard">
	<p id="leaderboard_header">Leaderboard</p>
	<div id="leaderboard_body">
		<% @users.each do |user| %>
			<div class="leaderboard_user">
				<div class="leaderboard_rank">
					<p><%= @users.index(user) + 1 %>.</p>
				</div>
				<div class="leaderboard_user_photo">
					<%= link_to user do %>
						<%= image_tag user.photo.url %>
					<% end %>
				</div>
				<div class="leaderboard_user_name">
					<p><%= link_to "#{user.name}", user %></p>
				</div>
				<div class="leaderboard_user_level">
					<p>Level <%= user.level %> <%= user.title %> (<%= user.points %> points)</p>
				</div>
			</div>
		<% end %>
	</div>
</div>
<script>
	$("#my_points_progress img").width(<%= @user.progress %> * 398)
</script>
