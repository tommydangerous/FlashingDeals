var $boxes = $('<%= j render :partial => "deals/box_deal", :collection => @deals, :as => :deal %>');
$("#grid_container").append( $boxes ).masonry( 'appended', $boxes);

<% if @deals.total_pages == @deals.current_page %>
	$(".feed_pagination").remove();
	$(".building_feed").remove();
<% else %>
	$(".building_feed").replaceWith("<%= j render("feed_pagination") %>");
<% end %>