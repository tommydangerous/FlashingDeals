<%= form_for current_user.relationships.build(:watched_id => @deal.id), :remote => true do |f| %>
	<div><%= f.hidden_field :watched_id %></div>
	<div class="actions">
		<%= f.submit "Watch", :onmouseover => "toggleWatchDealInfo()", :onmouseout => "toggleWatchDealInfo()" %>
	</div>
<% end %>