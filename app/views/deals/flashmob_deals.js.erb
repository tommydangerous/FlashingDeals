$("div#deals_list").append("<%= j render(@deals) %>");

<% if @deals.total_pages == @deals.current_page %>
	$(".pagination").remove();
<% else %>
	$("#loading_deals").replaceWith("<%= j will_paginate @deals, :separator => "" %>");
<% end %>