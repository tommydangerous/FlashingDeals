<%= render 'shared/side_panel' %>
<div id="deals_list">
	<div id="deal_page_info">
		<div id="show_electric_flashing">
			<ul>
				<span id="electric_flashing">
					<%= render "electric_flashing_sort" %>
				</span>
			</ul>
		</div>
		<%= link_to "", "javascript:void(0)", :id => "question", :onclick => "toggleDealPageInfo();" %>
		<h1>Featured Deals?</h1>
		<div class="deal_page_info">
			<div class="close_deal_page_info">
				<%= link_to image_tag("/assets/close.png"), "javascript:void(0);", :onclick => "toggleDealPageInfo();" %>
			</div>
			<p>FlashingDeals is always finding the best deals, but the home page can only fit so many at a time. This is where the Featured Deals page comes into play. If you missed any of the deals from the home page, you can come here and see them all! Want more? Check out the Community Deals for never ending excitement.</p>
		</div>
	</div>
	<div id="search">
		<% unless @deals.empty? %>
		<div class="paginate_range">
			<%= render 'paginate_range' %>
		</div>
		<% end %>
		<div id="search_form">
			<%= form_tag flashback_path, :method => :get, :id => "deals_search" do %>
				<%= text_field_tag :search, params[:search], :autocomplete => "off" %>
				<%= submit_tag "Search", :name => nil, :id => "search_button" %>
				<div id="sort_direction"><%= render 'sort_direction' %></div>
			<% end %>
		</div>
	</div>
	<div id="deals">
		<%= render 'flashback_deals' %>
	</div>
</div>
<script>
$(document).ready(function() {
	$("div#flashback_arrow").show();
	$("a#flashback").removeClass("not_selected");
	$("a#flashback").addClass("selected");
	$("div#progress")
		.hide()
		.ajaxStart(function() {
			$(this).show();
		})
		.ajaxStop(function() {
			$(this).hide();
		});
});
</script>