/*
$(document).ready(function() {
	setInterval("getMessageCount();", 10000);
});

function getMessageCount() {
	$.ajax({
		url: "/messages/get_message_count",
		cache: false,
		success: function(html){
			$("span.message_unread_count").html(html);
		}
	})
};
*/
